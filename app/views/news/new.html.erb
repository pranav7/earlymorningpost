<h1>Add News</h1>

<%= form_for(@news) do |f| %>
	<%= render 'shared/error_messages' %>
	<div class="fields">
		<%= render 'news_fields', :f => f %>		
	</div>
	<div class="actions">
		<%= f.submit "Save", :class => 'button' %>
	</div>
<% end %>