.main
  #page-title
    %h2= @category.name.upcase
- if admin?
  %p
    %strong Name:
    = @category.name
  = link_to 'Edit', edit_category_path(@category)
  |
  = link_to 'Back', categories_path

#cards-wrapper
  - @category.news.where(active: true).each_with_index do |news, index|
    - if index != 0 && index % 4 == 0
      = render '/pages/ad_card'

    = render '/pages/card', news: news

