<h1>Edit News</h1>

<%= form_for(@news) do |f| %>
	<%= render 'shared/error_messages' %>
	<%= render 'news_fields', :f => f %>		
	<div class="actions">
		<%= f.submit "Update", :class => 'button' %>
	</div>
<% end %>